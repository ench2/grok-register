[project]
name = "grok"
version = "0.1.0"
description = "Grok 自动化注册工具"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "curl-cffi==0.13.0",
    "datarecorder==3.6.2",
    "downloadkit==2.0.7",
    "drissionpage==4.1.1.3",
]
